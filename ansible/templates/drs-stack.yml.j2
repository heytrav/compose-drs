
version: "3.1"
services:
{% for service_name, spec in services.items() %} {% if service_name in volumes %} {% do spec.update('volumes', volumes.service_name) %} {% endif %}
  {{ {service_name: spec} | to_nice_yaml(width=50, explicit_start=False, explicit_end=False)  }}
{% endfor %}


secrets:
{% for secret in known_secrets %}
{% if secret in docker_secrets %}
 {{ secret }}: 
   {{ docker_secrets[secret] | to_nice_yaml(width=50, explicit_start=False, explicit_end=False) }}
{% endif %}{% endfor %}
