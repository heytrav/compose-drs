version: "2"
services:

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - 15672:15672
    env_file: .env

  centralnic:
    image: quay.io/heytrav/nodepp:latest
    command: ["node", "./lib/rabbit-epp.js", "-j", "-f", "epp-centralnic.json", "-r", "centralnic-test", "--loglevel", "debug"]
    volumes:
      - ../nodepp:/opt/project/node-epp
    links:
      - rabbitmq:rabbitmq
    depends_on:
      - rabbitmq
    env_file: .env

  api:
    image: domain-api
    ports:
      - "8000:8000"
    environment:
      - SQLITE_DIR=/var/domain-api/db
    volumes:
      - ../database/sqlite3:/var/domain-api/db
      - ../drs-project:/usr/local/domain-api/catalystinnovation
    command: ["python3", "manage.py", "runserver", "0.0.0.0:8000"]
    depends_on:
      - rabbitmq
    env_file: .env
    links:
      - rabbitmq:rabbitmq
